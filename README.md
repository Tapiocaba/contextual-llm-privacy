# contextual-llm-privacy

cs 279r final project built with React + TypeScript + Vite.

## Running locally

1. Install dependencies (first time only):
   ```sh
   npm install
   ```
2. Start the dev server:
   ```sh
   npm run dev
   ```
3. Open the URL (default `http://localhost:5173`) in your browser.
